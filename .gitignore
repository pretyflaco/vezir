# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.venv/
venv/
build/
dist/
.pytest_cache/

# Editor
.vscode/
.idea/
*.swp

# Vezir runtime data should never be committed
data/team.json
data/tokens.json
data/*.sqlite
data/sessions/
data/jobs/
data/logs/

# Local config
.env
*.local.json

# Operational session notes — contain real IPs, npubs, hostnames, and
# teammate-identifying data. Operator-local only; never publish.
docs/sessions/
# Root-level operator session transcripts (session-*.md) carry the same
# sensitive data as docs/sessions/ and must never be committed.
session-*.md

# AI coding assistant context — operator-local; references sensitive
# paths, infra topology, and cross-repo conventions not for publication.
AGENTS.md
.agents/
.claude/
_bmad/
.history/
